# @launchsim/mcp

An MCP server exposing one tool, `crash_test_scenario`, that runs a
launchsim scenario and returns the terminal report — the AI Infrastructure
half of the Monad hackathon's Trust/Identity & AI Infrastructure track
(docs/09).

## Docs

`docs/09-roadmap-mvp.md`.
