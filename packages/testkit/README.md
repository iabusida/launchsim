# @launchsim/testkit

Shared test builders (`aScenario()`, `aPool()`), fixtures, and a fake clock.
Dev-only: not published, not built, imported directly as TypeScript from
other packages' test suites.

## Docs

`docs/07-testing-tdd.md`.
