# @launchsim/blink

Hono app implementing the Solana Actions endpoint (GET/OPTIONS), `actions.json`,
and the report badge image, so a `RunResult` can be shared as a Blink. Never
signs or submits transactions.

## Docs

`docs/05-blink-actions.md`.
