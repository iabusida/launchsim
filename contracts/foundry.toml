[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.28"
optimizer = true
optimizer_runs = 200

[fuzz]
runs = 256

[invariant]
runs = 32
depth = 50

[profile.ci.fuzz]
runs = 1000

[profile.ci.invariant]
runs = 256
depth = 500

[fmt]
line_length = 100
